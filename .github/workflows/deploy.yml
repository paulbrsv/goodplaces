name: 🚀 Deploy to IONOS via FTP

on:
  push:
    branches:
      - main  # Деплой при пуше в main

jobs:
  ftp-deploy:
    name: 📂 Upload to FTP
    runs-on: ubuntu-latest  # Запуск на сервере GitHub

    steps:
      - name: 📥 Клонируем репозиторий
        uses: actions/checkout@v3

      - name: 🔄 Загружаем файлы на FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4  # Исправленная версия!
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: "./"  # Что загружаем
          server-dir: "/"  # Куда загружаем (замени на свой путь)
          exclude: |
            **/.git*
            **/.github*
            **/node_modules/*
            **/README.md
            **/.env
